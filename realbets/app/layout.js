export const metadata = {
  title: 'REALSPORTS.TOP',
  description: 'Sports Betting Platform',
}

import './globals.css'

export default function RootLayout({ children }) {
  return (
    <html lang="pt-br">
      <body>{children}</body>
    </html>
  )
}
